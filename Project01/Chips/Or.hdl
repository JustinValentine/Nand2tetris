/** returns 1 if at least one of {a,b} is a 1, else returns 0 */

CHIP Or {
    IN a, b; 
    OUT out; 

    PARTS: 
    Nand(a=a, b=a, out=c0); 
    Nand(a=b, b=b, out=c1); 
    Nand(a=c0, b=c1, out=out);  
}        